import Card from '../ui/Card';

const ExtraCard = () => {
  return (
    <Card>
      <div className="h-full flex items-center justify-center">
        <h2 className="text-2xl font-bold text-white">Дополнительная карточка</h2>
      </div>
    </Card>
  );
};

export default ExtraCard;
