import Card from '../ui/Card';

const SecondaryCard = () => {
  return (
    <Card>
      <div className="flex items-center justify-center h-full">
        <span className="text-gray-400">Дополнительный контент</span>
      </div>
    </Card>
  );
};

export default SecondaryCard;
